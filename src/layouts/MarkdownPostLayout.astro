---
import TagButton from "../components/TagButton.astro";
import "../styles/global.css";
import BaseLayout from "./BaseLayout.astro";
const { frontmatter } = Astro.props;
const dateString = new Date(frontmatter.pubDate).toLocaleString("en-EN", {
    year: "numeric",
    month: "long",
    day: "numeric",
});
const { title, description, image, author, tags } = frontmatter;
---

<BaseLayout pageTitle={title} description={description} image={image}>
    <p>Written by {author}</p>
    <p>Published on: {dateString}</p>
    <p><em>{description}</em></p>
    <img class="my-2" src={image.url} width="300" alt={image.alt} />
    {
        tags && (
            <p class="flex flex-wrap gap-1 my-2">
                {tags.map((tag: string, index: number) => (
                    <TagButton tag={tag} />
                ))}
            </p>
        )
    }
    <slot />
</BaseLayout>
